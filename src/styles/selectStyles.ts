


export const customStyles = {
  control: (base: any, state: any) => ({
    ...base,
    background: '#000046',
    width: '15rem',
    text: '#a0a0a0',
    border: 'none',
    // fontColor: '#fff',
    boxShadow: 'none',
    // textColor: '#fff'
  }),
  option: (base: any) => ({
    ...base,
    color: '#000',
    background: '#fff',
  }),
  singleValue: (base: any) => ({
    ...base,
    color: '#fff'
  })
}